<form method="post" action="{{ path('email_address_delete', {'id': email_address.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    {#<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ email_address.id) }}">#}
    {#<button class="btn">{{ 'action.delete' | trans }}</button>#}
</form>

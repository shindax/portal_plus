{% extends '@EasyAdmin/default/list.html.twig' %}

{% block item_actions %}

	{# % if item.AuthorID is not null %}
		{{dump(item.AuthorID.Name)}}
	{% endif % #}

    {% if  app.user.OrgUserID.ID != item.AuthorID.id %}
        {%  set _list_item_actions = _list_item_actions|slice(1,1) %}
    {% endif %}

    {{ parent() }}
{% endblock %}

